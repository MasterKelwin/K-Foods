import styled from 'styled-components';

export const Wrapper = styled.ul `
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    margin-top: 1rem;

    li {
        margin-bottom: 1rem;
    }
`